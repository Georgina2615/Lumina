import { PublicContactSection } from '../components';

// Compone la pantalla publica de contacto
export default function PublicContactPage() {
  // Devuelve los canales confirmados de Lumina Skin
  return <PublicContactSection />;
}
