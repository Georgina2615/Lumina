import { Outlet } from 'react-router-dom';

// Delimita las capacidades protegidas de cabina
export default function ClinicalLayout() {
  // Presenta la capacidad clínica seleccionada
  return <Outlet />;
}
